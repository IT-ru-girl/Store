import { HeaderBar, HeaderTitle } from './Header.styles.tsx';



export const Header = () => {

  return (
      <HeaderBar>
        <HeaderTitle variant="h5" noWrap color="inherit">
          Store
        </HeaderTitle>
      </HeaderBar>
  );
};
